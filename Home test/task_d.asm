section .text
global _start

;==========================================================================

_start:
        mov rax, 2
        mov rbx, 3

        call main
        call end

;==========================================================================

main:
       
        mov rcx, number
        imul rbx, number
        add rax, rbx
        mov rdx, rax

;==========================================================================

end:
        mov rax, 60
        xor rdi, rdi
        syscall
        ret

;==========================================================================

section .data

numer equ 4

;==========================================================================
